This program monitors clinician locations and sends email alerts when anyone leaves a designated zone.

## Setup
1. Clone this repo
2. Run `npm install`
3. Create a `.env` file with:
   - `EMAIL_USER=your-gmail@gmail.com`
   - `EMAIL_PASS=your-app-password`
4. Run `npm start`

## Testing
Run `npm test`